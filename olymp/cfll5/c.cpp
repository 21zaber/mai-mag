#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int n,m;
    cin >> n >> m;

    for (int i = 0; i < n; ++i) {
        int a;
        cin >> a;
        e.push_back(make_pair(a, 0));
    }
    for (int i = 0; i < m; ++i) {
        int a, b, c;
        cin >> a >> b >> c;

        e.push_back(make_pair(a


    vector<pair<int, int> > e;




}
